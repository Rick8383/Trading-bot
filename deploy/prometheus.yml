# Prometheus scrape config. The API exposes /prometheus (text exposition).
global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "tradingbot"
    metrics_path: /prometheus
    static_configs:
      - targets: ["api:8000"]
