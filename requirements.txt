# --- Core (required for the deterministic engine + tests) ---
numpy>=1.26
pandas>=2.1
pydantic>=2.6
PyYAML>=6.0
structlog>=24.1

# --- API / monitoring ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Optional: market data connectors (install when needed) ---
# ccxt>=4.2            # crypto exchanges
# yfinance>=0.2        # equities/ETF historical data

# --- Optional: heavy backtesting / ML (Phase 2+) ---
# vectorbt>=0.26
# scipy>=1.11
# scikit-learn>=1.4
# xgboost>=2.0
# lightgbm>=4.3

# --- Dev / test ---
pytest>=8.0
httpx>=0.27
